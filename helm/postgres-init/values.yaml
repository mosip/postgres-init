# Default values for postgres-init.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: mosipqa/postgres-init
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: develop

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

jobAnnotations: {}

jobSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
# runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
  # kubernetes.io/ingress.class: nginx
  # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
#   cpu: 100m
#   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

## Some common set of users are created across all postgres servers with same password.
dbUserPasswords:
  dbuserPassword: ""

## Per-database configuration:
##
## enabled:        Set to true to run the init job for this database.
## scriptsDir:     Folder name inside db_scripts/ in the git repo. MUST match the repo
##                 structure exactly (e.g. mosip_master). Do NOT change this unless the
##                 repo folder is renamed.
## dbName:         Actual Postgres database name to create. Can be anything
##                 (e.g. mosip_master, mosip_master02). This is what gets created in Postgres.
## dbUser:         The DB user created/used by deploy.sh (DB_UNAME). Can be any name
##                 (e.g. masteruser, masteruser02).
## defaultDb:      Default postgres DB to connect to before creating the target DB.
## host:           In-cluster service name of postgres.
## port:           Postgres port (default 5432).
## su.user:        Postgres superuser name (default: postgres).
## su.secret.name: K8s secret name holding the superuser password.
## su.secret.key:  Key inside the secret for the superuser password.
## dml:            Whether DML scripts should be loaded: 0 (skip) or 1 (load).
## repoUrl:        Git repo URL containing the db_scripts/ folder.
## branch:         Git branch to clone from.

databases:
  mosip_master:
    enabled: false
    scriptsDir: mosip_master      # fixed - must match db_scripts/ folder in repo
    dbName: mosip_master          # customize e.g. mosip_master02
    dbUser: masteruser            # customize e.g. masteruser02
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/admin-services.git
    branch: develop

  mosip_audit:
    enabled: false
    scriptsDir: mosip_audit
    dbName: mosip_audit
    dbUser: audituser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/audit-manager.git
    branch: develop

  mosip_keymgr:
    enabled: false
    scriptsDir: mosip_keymgr
    dbName: mosip_keymgr
    dbUser: keymgruser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/keymanager.git
    branch: develop

  mosip_kernel:
    enabled: false
    scriptsDir: mosip_kernel
    dbName: mosip_kernel
    dbUser: kerneluser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/commons.git
    branch: develop

  mosip_idmap:
    enabled: false
    scriptsDir: mosip_idmap
    dbName: mosip_idmap
    dbUser: idmapuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/id-repository.git
    branch: develop

  mosip_prereg:
    enabled: false
    scriptsDir: mosip_prereg
    dbName: mosip_prereg
    dbUser: prereguser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/pre-registration.git
    branch: develop

  mosip_idrepo:
    enabled: false
    scriptsDir: mosip_idrepo
    dbName: mosip_idrepo
    dbUser: idrepouser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/id-repository.git
    branch: develop

  mosip_ida:
    enabled: false
    scriptsDir: mosip_ida
    dbName: mosip_ida
    dbUser: idauser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/id-authentication.git
    branch: develop

  mosip_credential:
    enabled: false
    scriptsDir: mosip_credential
    dbName: mosip_credential
    dbUser: credentialuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/id-repository.git
    branch: develop

  mosip_regprc:
    enabled: false
    scriptsDir: mosip_regprc
    dbName: mosip_regprc
    dbUser: regprcuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/registration.git
    branch: develop

  mosip_pms:
    enabled: false
    scriptsDir: mosip_pms
    dbName: mosip_pms
    dbUser: pmsuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    # Note: This flag is used to update datashare domain in partner policies according to deployment version (i.e v2/v3)
    # Set flag ENV_VERSION_FLAG to true for v2 envs and false for v3 envs.
    ENV_VERSION_FLAG: false
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/partner-management-services.git
    branch: develop

  mosip_hotlist:
    enabled: false
    scriptsDir: mosip_hotlist
    dbName: mosip_hotlist
    dbUser: hotlistuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/admin-services.git
    branch: develop

  mosip_resident:
    enabled: false
    scriptsDir: mosip_resident
    dbName: mosip_resident
    dbUser: residentuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/resident-services.git
    branch: develop

  mosip_toolkit:
    enabled: false
    scriptsDir: mosip_toolkit
    dbName: mosip_toolkit
    dbUser: toolkituser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/mosip-compliance-toolkit.git
    branch: develop

  mosip_digitalcard:
    enabled: false
    scriptsDir: mosip_digitalcard
    dbName: mosip_digitalcard
    dbUser: digitalcarduser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/digital-card-service.git
    branch: develop

  mosip_esignet:
    enabled: false
    scriptsDir: mosip_esignet
    dbName: mosip_esignet
    dbUser: esignetuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/esignet.git
    branch: develop

  mosip_mockidentitysystem:
    enabled: false
    scriptsDir: mosip_mockidentitysystem
    dbName: mosip_mockidentitysystem
    dbUser: mockidentityuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/esignet-mock-services.git
    branch: develop

  mosip_otp:
    enabled: false
    scriptsDir: mosip_otp
    dbName: mosip_otp
    dbUser: otpuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 0
    repoUrl: https://github.com/mosip/otp-manager.git
    branch: develop

  inji_certify:
    enabled: false
    scriptsDir: inji_certify
    dbName: inji_certify
    dbUser: injicertifyuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/inji-certify.git
    branch: develop

  inji_verify:
    enabled: false
    scriptsDir: inji_verify
    dbName: inji_verify
    dbUser: injiverifyuser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/inji-verify.git
    branch: develop

  inji_mimoto:
    enabled: false
    scriptsDir: inji_mimoto
    dbName: inji_mimoto
    dbUser: injimimotouser
    defaultDb: postgres
    host: "postgres-postgresql"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/mimoto.git
    branch: develop
